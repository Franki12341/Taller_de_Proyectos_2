# Evidencias de base de datos

Documenta el estado real de `/api/health`, la conexión a PostgreSQL cuando esté disponible y el comportamiento de fallback.

- No se exponen credenciales ni cadenas de conexión.
- Los archivos en `simulacion/` se usan solo cuando no puede verificarse una conexión PostgreSQL real.
- `EV-DB-06-fallback.md` puede existir como evidencia real del modo degradado.
